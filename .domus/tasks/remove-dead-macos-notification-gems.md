# Task: Remove dead macOS notification gems

**ID:** remove-dead-macos-notification-gems
**Status:** done
**Autonomous:** true
**Priority:** low
**Captured:** 2026-04-10
**Parent:** none
**Depends on:** none
**Idea:** none
**Spec refs:** none

---

## What This Task Is

`ruby_gntp` (Growl — discontinued 2020) and `terminal-notifier-guard` (unmaintained) are dead notification gems. The Guard gems that consume them (`guard-bundler`, `guard-rails`, `guard-rspec`) are also in maintenance mode and not actively used — the workflow is superseded by IDE test runners and Vitest watch mode. Remove all five gems and the Guardfile.

---

## Acceptance Criteria

- [x] `ruby_gntp` removed from Gemfile
- [x] `terminal-notifier-guard` removed from Gemfile
- [x] `guard-bundler` removed from Gemfile
- [x] `guard-rails` removed from Gemfile
- [x] `guard-rspec` removed from Gemfile
- [x] `Guardfile` deleted (if it exists at repo root)
- [x] `Gemfile.lock` updated via `bundle install`
- [x] `bundle exec rspec` passes
- [x] No other code references these gems (grep to confirm)

---

## Implementation Notes

### Files to change
- `Gemfile` — remove 5 gems (lines 66-73 area, all in dev/test group)
- `Guardfile` — delete if present
- `Gemfile.lock` — regenerated by `bundle install`

### Approach
- Remove the gem lines, run `bundle install`, verify no code references remain, run rspec
- These are all in the development/test group — no production impact

### Risks
- None. Growl is dead, Guard is unused.

### Commit scope
- Single commit
